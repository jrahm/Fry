package main

struct String
end

struct List(T)
end

struct Int
end

struct Unit
end

fn [A: Any] singleton(x: A) -> List(A)
end

fn println(arg: String) -> Unit
    main(singleton(arg))
end

fn main(argv: List(String)) -> Int
    println("Hello, World!")
end
