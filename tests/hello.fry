package main

infixr >> 50 seq

fn main(test: T -> G, testa: A -> B)
    println()

    fn nother(q: List)
        println(4, 2 + 3 * 4)
        println()
        println()
    end

    (nother() >> nother()) >> nother() >> nother()

end

fn collatz(n: Int) -> Int
    if n == 1
        0
    else
        if n % 2 = 0
            collatz(n / 2)
        else
            collatz(1 + n * 3)
        end
    end
end
